import { LocalResponse } from "@/input/index.js";

/**
 * Local response loaded from a file.
 * Note: Has to be initialized through init() before use.
 */
export class LocalResponseV1 extends LocalResponse { }
